#include <stdio.h>
#include "Book_Program02.h"

int main(void){
    int digitNumber = 456;
    printf("The sum of the 3 digits in the number %d is %d", digitNumber, sum3Digits(digitNumber));

    return 0;
}